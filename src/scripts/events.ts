const PRELOAD = 'PRELOAD';
const MAINMENU = 'MAINMENU';
const GAME = 'GAME';

const Scenes = {
  PRELOAD,
  MAINMENU,
  GAME,
};

const GameEvents = {
  BOARD_GENERATED: 'BOARD_GENERATED',
};

export { Scenes, GameEvents };
